# NetLearner 项目计划书 v4.0（重构版）

> 吸取 v1-v3 所有教训，从零重建。

---

## 一、历史上踩过的坑（必须解决）

| 问题 | 根因 | 本版解决方案 |
|---|---|---|
| **CCNA 模拟考显示 32 题而不是 60 题** | 浏览器缓存了旧版 JSON + 两个服务器进程冲突 | ① 唯一启动方式 `node server.js` ② 服务器返回 `Cache-Control: no-store` ③ fetch 带版本号参数 `?t=APP_VERSION` |
| **CCNP 水平测试在 Edge 中显示"待推出"** | Edge 缓存了文件创建前的 404 响应 | 同上三层防缓存 |
| **Chrome 和 Edge 表现不一致** | 不同浏览器在不同时间请求，各自缓存不同版本 | 唯一服务器进程 + 无缓存头（各浏览器统一标准） |
| **水平测试选完不能返回** | 缺少重置 UI 的机制 | `lt-setup` / `lt-exam` 两个容器切换，导航到页面时自动 `resetLevelTest()` + 界面上「⬅ 重新选择」按钮 |
| **启动方式混乱** | 先后用了 `python -m http.server`、`npx http-server`、`node server.js`，互相干扰 | 只有一种启动方式：`node server.js` |
| **项目文件结构混乱** | 开发过程中不断打补丁，脚本、构建工具占据根目录 | 重构时彻底清理，只保留必要的文件 |

---

## 二、开发规范

### 2.1 服务器

```bash
# 唯一启动命令（写死在文档里，不出现第二种）
node server.js
```

server.js 的行为：
- 监听 8080
- 所有响应带 `Cache-Control: no-store`
- 不依赖任何 npm 包
- 只有一个进程

### 2.2 缓存控制

```
服务器层: Cache-Control: no-store, no-cache, must-revalidate
          Pragma: no-cache
          Expires: 0
客户端层: fetch(url + '?t=' + APP_VERSION)
          APP_VERSION = 硬编码在 app.js 顶部
```

### 2.3 页面状态管理

每个页面在切换时自动重置：
- `page-level-test` → `resetLevelTest()` 显示选择界面
- `page-exam` → `renderExamList()` 列出可用试卷
- `page-wrong-answers` → `renderWrongAnswers()` 重新渲染

### 2.4 题库元数据结构

```json
{
  "id": "ccna-mock-1",
  "title": "CCNA 模拟考试卷一",
  "type": "mock-exam",
  "target": "CCNA",
  "questionCount": 60,
  "timeLimit": 120,
  "difficultyDistribution": { "easy": 18, "medium": 30, "hard": 12 }
}
```

所有题量数字与实际 JSON 文件的 questionCount 字段严格一致。

---

## 三、考试规格（真实数据）

### CCNA 200-301

| 知识域 | 权重 | 模拟题量 |
|---|---|---|
| Network Fundamentals | 20% | 12 题 |
| Network Access | 20% | 12 题 |
| IP Connectivity | 25% | 15 题 |
| IP Services | 10% | 6 题 |
| Security Fundamentals | 15% | 9 题 |
| Automation | 10% | 6 题 |
| **总计** | **100%** | **60 题** |

真实考试：~100 题 / 120 分钟 / 825 分通过

### CCNP ENCOR 350-401 v1.2

| 知识域 | 权重 | 模拟题量 |
|---|---|---|
| Architecture | 15% | 9 题 |
| Virtualization | 10% | 6 题 |
| Infrastructure | 30% | 18 题 |
| Network Assurance | 10% | 6 题 |
| Security | 20% | 12 题 |
| Automation & AI | 15% | 9 题 |
| **总计** | **100%** | **60 题** |

真实考试：~100 题 / 120 分钟 / 825 分通过

---

## 四、构建清单

### 第一批：基础设施

- [ ] `server.js` — Node.js 纯内建 HTTP 服务器，无缓存头
- [ ] `.gitignore` — 忽略 node_modules 等
- [ ] `index.html` — SPA 框架，hash 路由
- [ ] `assets/css/main.css` — 完整设计系统（暗色主题 + Cisco 蓝）
- [ ] `assets/css/responsive.css` — 响应式（375px+）
- [ ] `assets/js/router.js` — 哈希路由
- [ ] `assets/js/storage.js` — 存储层（localStorage + 导出/导入 JSON）

### 第二批：考试引擎

- [ ] `assets/js/exam-engine.js` — 考试状态机
- [ ] `assets/js/wrong-answer-service.js` — 错题本
- [ ] `assets/js/planner-generator.js` — 学习计划
- [ ] `assets/js/app.js` — 主逻辑（页面渲染、事件绑定、数据加载）

### 第三批：题库

- [ ] `questions/index.json` — 元数据索引
- [ ] `questions/generated/ccna-level-demo.json` — CCNA 水平测试 10 题
- [ ] `questions/generated/ccna-mock-1.json` — CCNA 模拟考 60 题
- [ ] `questions/generated/ccnp-encor-level-demo.json` — CCNP 水平测试 10 题
- [ ] `questions/generated/ccnp-encor-mock-1.json` — CCNP 模拟考 60 题

### 第四批：校验

- [ ] 所有页面导航正确
- [ ] 水平测试 → 答题 → 提交 → 结果 → 学习计划
- [ ] 模拟考试 → 60 题显示 → 计时 → 交卷
- [ ] 错题本 → 收录 → 筛选 → 重练 → 导出
- [ ] `node server.js` → Chrome/Edge 打开 → 完全一致
- [ ] 首页标语显示正确

---

## 五、验收标准

```
□ node server.js 启动后，Chrome 和 Edge 同时打开表现一致
□ 水平测试可选择 CCNA / CCNP ENCOR
□ 水平测试中有「⬅ 重新选择」按钮
□ CCNA 模拟考试 = 60 题，CCNP 模拟考试 = 60 题
□ 提交后错题自动收录到错题本
□ 导出 JSON / 导入 JSON 功能可用
□ 首页标题为「考纲驱动 · AI 动态出题」
□ 所有页面无控制台报错
```
