# NetLearner 使用教程

> 从零开始，学会使用 NetLearner 备考 CCNA / CCNP

---

## 目录

- [一、这是什么](#一这是什么)
- [二、PC 端使用](#二pc-端使用)
- [三、手机端使用（同一 WiFi）](#三手机端使用同一-wiFi)
- [四、功能详解](#四功能详解)
- [五、数据管理](#五数据管理)
- [六、常见问题](#六常见问题)

---

## 一、这是什么

NetLearner 是一个**开箱即用的 CCNA/CCNP 备考工具**：

- 📖 **考纲驱动** — 所有题目基于 Cisco 官方 Exam Topics 生成
- 🤖 **AI 动态出题** — 不依赖任何静态题库，零版权风险
- 🔒 **数据本地私有** — 你的学习数据存在你自己浏览器的 localStorage 中
- ⚡ **纯前端** — 无需安装任何软件，一个浏览器就能用

### 适用人群

- 准备 CCNA 200-301 认证的考生
- 准备 CCNP ENCOR 350-401 认证的考生
- 自测网络知识水平的技术人员

---

## 二、PC 端使用

### 2.1 在电脑上启动

#### 前提条件

- Windows / macOS / Linux 电脑
- [Node.js](https://nodejs.org/) 已安装（验证：在终端执行 `node --version`）

#### 启动步骤

```
第 1 步：打开终端（命令行）
  Windows: 按 Win+R → 输入 cmd → 回车
  macOS:   启动台 → 终端
  Linux:   Ctrl+Alt+T

第 2 步：进入项目目录
  cd 你存放NetLearner的路径
  例如：cd E:\NetLearner

第 3 步：启动服务器（只有一个命令）
  node server.js

  看到以下输出表示成功：
  NetLearner → http://localhost:8080
  Cache: DISABLED (all browsers)

第 4 步：在浏览器打开
  Chrome / Edge / Firefox 访问：
  http://localhost:8080
```

> ⚠️ 不要用 `http-server` 或 `python -m http.server`，项目只支持 `node server.js`。

### 2.2 PC 端功能导航

打开后你会看到顶部导航栏：

```
首页  |  水平测试  |  模拟考试  |  错题本  |  学习计划  |  设置
```

| 功能 | 怎么用 |
|---|---|
| **首页** | 查看学习统计数据，快速进入各功能 |
| **水平测试** | 选择 CCNA 或 CCNP → 答 10 题 → 提交 → 获得等级和学习计划 |
| **模拟考试** | 选择 CCNA（60 题）或 CCNP（60 题）→ 120 分钟计时答题 → 自动判分 |
| **错题本** | 自动收录做错的题 → 筛选/标记掌握状态 → 错题重练 |
| **学习计划** | 根据水平测试等级生成 12/8/4 周备考计划 |
| **设置** | 切换深色/浅色主题、导出/导入数据、清除数据 |

---

## 三、手机端使用（同一 WiFi）

这是**不需要部署到服务器**就能在手机上使用的方案，适合在通勤、午休时刷题。

### 3.1 前提条件

```
□ 电脑已启动 NetLearner（node server.js 运行中）
□ 手机和电脑连接同一个 WiFi（同一个路由器）
□ 电脑的防火墙允许 8080 端口
```

### 3.2 找到电脑的局域网 IP

在电脑终端执行：

```bash
# Windows
ipconfig | findstr "IPv4"

# macOS / Linux
ifconfig | grep inet
```

找到类似 `192.168.x.x` 的地址，记下来。

> **常见 WiFi IP 段：**
> - 192.168.1.x（TP-Link / 小米路由器）
> - 192.168.31.x（小米路由器）
> - 192.168.0.x（某些华为路由器）
> - 10.0.0.x（某些企业网络）

### 3.3 手机上打开

```
第 1 步：手机连上同一个 WiFi
第 2 步：打开手机浏览器（Chrome / Edge / Safari 均可）
第 3 步：地址栏输入 http://192.168.x.x:8080
         （x.x 替换成你查到的数字）
第 4 步：回车，看到 NetLearner 首页即成功！
```

**示例**：如果电脑 IP 是 `192.168.31.88`，手机浏览器输入 `http://192.168.31.88:8080`

### 3.4 手机端操作提示

```
📱 水平测试 / 模拟考试
  - 点选项答题（手指触摸选）
  - 点「下一题」切换
  - 点「提交」交卷

📱 题目太长怎么办？
  - 横屏模式可以显示更多内容
  - 手机 Safari 可以启用「阅读器视图」

📱 错题本
  - 上下滑动浏览错题
  - 点「需要复习 / 基本掌握 / 已掌握」标记状态
```

### 3.5 防火墙设置（如果手机打不开）

**Windows 防火墙放行 8080 端口：**

```
第 1 步：打开 Windows 安全中心
         Win+S 搜索 "Windows 安全中心"

第 2 步：防火墙和网络保护 → 高级设置

第 3 步：入站规则 → 新建规则

第 4 步：规则类型 → 端口 → 下一步

第 5 步：TCP → 特定本地端口 → 输入 8080 → 下一步

第 6 步：允许连接 → 下一步

第 7 步：勾选 专用 / 公用 → 下一步

第 8 步：输入名称 "NetLearner" → 完成
```

**macOS 防火墙放行：**

```
系统设置 → 网络 → 防火墙 → 关闭（或添加例外）
```

---

## 四、功能详解

### 4.1 水平测试

```
目的：快速评估你的当前水平

流程：
  1. 点导航栏「水平测试」
  2. 选择「CCNA」或「CCNP ENCOR」
  3. 逐题作答（10 题，不可回退）
  4. 答完点「提交」
  5. 查看结果页面 →
     - 得分（百分比）
     - 等级（初级 / 中级 / 高级）
     - 各知识域正确率
     - 每个知识域的颜色块
  6. 点「学习计划」查看备考计划
  7. 点「错题」复习做错的题

提示：
  - 测试过程中可以点「⬅ 重新选择」返回重新选认证类型
  - 提交后系统自动生成学习计划
```

### 4.2 模拟考试

```
目的：全真模拟 CCNA / CCNP 考试

流程：
  1. 点导航栏「模拟考试」
  2. 选择 CCNA（60 题·120 分钟）或 CCNP（60 题·120 分钟）
  3. 答题 →
     - 点选项选择答案
     - 点「上一题」「下一题」导航
     - 点「🏴 标记」标记不确定的题
     - 点「📋 答题卡」快速跳转到任意题号
  4. 全部答完或时间到 → 自动交卷
  5. 查看结果 →
     - 得分
     - 各知识域正确率柱状图
     - 错题自动收录到错题本

提示：
  - 交卷前可以随时返回修改答案
  - 考试中意外关闭页面 → 进度丢失（建议一次完成）
  - 点「⬅ 返回试卷列表」可切换试卷
```

### 4.3 错题本

```
目的：集中复习做错的题

功能：
  顶部统计 → 总错题数 / 需复习 / 基本掌握 / 已掌握
  筛选 → 按知识域、按掌握状态
  每条错题显示 →
    - 题目原文
    - 你的答案（红色 ❌）
    - 正确答案（绿色 ✅）
    - 详细解析
    - 掌握状态按钮（仍需复习 / 基本掌握 / 已掌握）
    - 删除按钮

操作：
  - 点掌握状态按钮 → 标记这道题的掌握程度
  - 点「🔄 错题重练」→ 把未掌握的错题组成一张新卷子重新做
  - 点「📤 导出」→ 下载错题 JSON 备份
  - 点「🗑️ 清空」→ 清空所有错题（建议先导出）
```

### 4.4 学习计划

```
目的：根据水平测试结果定制备考计划

生成规则：
  初级（得分 < 60%）→ 12 周计划，从基础开始
  中级（得分 60%-84%）→ 8 周计划，跳过基础
  高级（得分 ≥ 85%）→ 4 周计划，聚焦弱域 + 模考

每周计划包含：
  - 每天的学习任务（阅读 / 练习 / 实验）
  - 预计耗时（分钟）
  - 每周里程碑

操作：
  - 点「清除」删除当前计划（之后可重新生成）
```

### 4.5 设置

```
主题切换 → 深色 / 浅色
数据管理 →
  📤 导出全部数据 → 下载 JSON 文件（含错题本+考试记录+设置+计划）
  📥 导入数据 → 从 JSON 文件恢复数据
  🗑️ 清除所有数据 → 删除浏览器中全部 NetLearner 数据
```

---

## 五、数据管理

### 5.1 数据存在哪里

```
所有数据存在你当前浏览器的 localStorage 中。

Chrome:   设置 → 隐私和安全 → Cookie 及其他网站数据 → 查看 Cookie
Edge:     设置 → Cookie 和网站权限 → Cookie 和网站数据
Firefox:  设置 → 隐私与安全 → Cookie 和站点数据

具体存储的 key（以 nl_ 开头）：
  nl_wa          → 错题本数据
  nl_recs        → 考试记录
  nl_settings    → 设置（主题等）
  nl_plan        → 学习计划
```

### 5.2 数据备份

```
建议每次刷完题后：

  设置页 → 点「📤 导出全部数据」
  → 浏览器下载 NetLearner_YYYY-MM-DD.json
  → 把这个文件存到 iCloud / Dropbox / 微信收藏 / U盘
```

### 5.3 换设备迁移

```
旧设备：
  设置页 → 导出数据 → 得到 .json 文件

新设备：
  启动 NetLearner → 设置页 → 导入数据 → 选择 .json 文件
  → 所有错题本、考试记录、设置全部恢复
```

---

## 六、常见问题

### Q1: 启动时提示端口被占用

```bash
# 错误信息：EADDRINUSE :::8080

# 解决方案：杀掉占用 8080 端口的进程
# Windows（管理员运行 cmd）
netstat -ano | findstr :8080
taskkill /F /PID 进程ID

# macOS / Linux
lsof -i :8080
kill -9 PID

# 然后重新启动
node server.js
```

### Q2: 手机打开显示 "无法访问此网站"

```
检查清单：
  □ 电脑和手机连的是同一个 WiFi？
  □ 电脑上 node server.js 还在运行？
  □ 输入的地址是 http:// 不是 https://？
  □ 输入的 IP 地址正确？（在电脑上用 ipconfig 查）
  □ 防火墙放行了 8080 端口？
```

### Q3: 页面显示 "题库加载失败"

```
原因：
  服务器未运行，或文件路径不对

解决方案：
  □ 确认 node server.js 正常运行中
  □ 刷新页面（浏览器会重新请求）
  □ 检查终端窗口有没有报错
```

### Q4: 错题不见了 / 数据丢失

```
原因：
  清除了浏览器缓存 / 换了浏览器 / 用了隐私模式

解决方案：
  □ 如果有导出备份 → 设置 → 导入数据
  □ 如果没有备份 → 数据无法恢复（这就是为什么建议定期导出）
```

### Q5: 如何清除手机浏览器的缓存

```
Chrome（Android）:
  设置 → 隐私和安全 → 清除浏览数据 → 缓存的图片和文件

Safari（iPhone）:
  设置 → Safari → 清除历史记录和网站数据
```

### Q6: 模拟考试时间到了会怎样？

```
计时器到 0 时自动交卷，未答题自动计为错误。
你可以在交卷确认弹窗中看到未答题数量。
```

### Q7: 题目不够做怎么办？

```
NetLearner 的题目由 AI 基于官方考纲生成。
需要新题时：
  1. 更新考纲文本
  2. 运行 AI 出题 Prompt
  3. 将生成的 JSON 放入 questions/generated/
  4. 刷新页面即可使用
```

---

## 附录

### 命令速查

```bash
# 启动
node server.js

# 查找电脑 IP（Windows）
ipconfig | findstr "IPv4"

# 查找电脑 IP（macOS / Linux）
ifconfig | grep inet

# 杀掉占用 8080 端口的进程（Windows）
netstat -ano | findstr :8080
taskkill /F /PID 进程号

# 杀掉占用 8080 端口的进程（macOS / Linux）
lsof -i :8080
kill -9 PID
```

### 支持的浏览器

| 浏览器 | PC 端 | 手机端 |
|---|---|---|
| Chrome | ✅ | ✅ Android |
| Edge | ✅ | ❌ |
| Safari | ✅ | ✅ iPhone |
| Firefox | ✅ | ✅ Android |

### 文件下载

NetLearner 项目地址：GitHub 搜索 `NetLearner`（开源 / MIT License）
